# Copy to .env and edit as needed.
# Override the SQLite DB path inside the backend container:
# PFT_DB_URL=jdbc:sqlite:/data/pft.db

# Allow additional CORS origins (comma-separated):
# PFT_CORS_ORIGINS=http://localhost:5173
